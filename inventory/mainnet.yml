---
all:
  vars:
    env: mainnet
  children:
    supra:
      hosts:
        h1.finland.hub.mainnet.encapsulate.xyz:
          type: validator
        fullnode.supra.mainnet.encapsulate.xyz:
          type: fullnode
